package com.fansauchiwa.ui.theme

import androidx.compose.ui.unit.dp

// ============================================================
// Spacing (8-Point Grid System)
// ============================================================
/** 0dp — zero spacing */
val SpacingNone = 0.dp

/** 4dp — extra-small spacing (fine adjustments only) */
val SpacingXxs = 4.dp

/** 8dp — small spacing */
val SpacingXs = 8.dp

/** 12dp — small-medium spacing (fine adjustment) */
val SpacingSm = 12.dp

/** 16dp — medium spacing */
val SpacingMd = 16.dp

/** 24dp — large spacing */
val SpacingLg = 24.dp

/** 32dp — extra-large spacing */
val SpacingXl = 32.dp

/** 48dp — 2x extra-large spacing */
val SpacingXxl = 48.dp

// ============================================================
// Corner Radius
// ============================================================
/** 2dp — minimal corner radius */
val CornerRadiusXs = 2.dp

/** 4dp — small corner radius */
val CornerRadiusSm = 4.dp

/** 8dp — medium corner radius */
val CornerRadiusMd = 8.dp

/** 16dp — large corner radius */
val CornerRadiusLg = 16.dp

// ============================================================
// Icon Sizes
// ============================================================
/** 16dp — extra-small icon */
val IconSizeXs = 16.dp

/** 24dp — standard icon */
val IconSizeMd = 24.dp

/** 36dp — medium-large icon / color swatch */
val IconSizeLg = 36.dp

/** 48dp — large icon / loading indicator */
val IconSizeXl = 48.dp

// ============================================================
// Stroke / Border Widths
// ============================================================
/** 1dp — thin border / guideline */
val StrokeWidthThin = 1.dp

/** 2dp — normal border */
val StrokeWidthNormal = 2.dp

/** 4dp — thick stroke */
val StrokeWidthThick = 4.dp

// ============================================================
// Component Sizes
// ============================================================
/** 24dp — decoration gesture-input handle size */
val DecorationHandleSize = 24.dp

/** 48dp — touch-target minimum height / drag handle height */
val TouchTargetHeight = 48.dp

/** 4dp — drag handle visual width */
val DragHandleWidth = 4.dp

/** 56dp — image grid cell minimum size */
val ImageGridCellSize = 56.dp

/** 64dp — default image decoration size */
val ImageSizeDefault = 64.dp

/** 72dp — thumbnail size */
val ThumbnailSize = 72.dp

/** 104dp — color-picker slider track width */
val ColorPickerSliderWidth = 104.dp

/** 120dp — large image decoration placeholder */
val ImageSizeLarge = 120.dp

/** 152dp — home grid cell adaptive size */
val HomeGridCellSize = 152.dp

/** 300dp — home grid card minimum height */
val HomeGridCardMinHeight = 300.dp

// ============================================================
// Ad Banner
// ============================================================
/** 50dp — AdMob banner standard height */
val AdBannerHeight = 50.dp

// ============================================================
// Button
// ============================================================
/** 88dp — minimum button width */
val ButtonMinWidth = 88.dp

/** 56dp — standard button height (nearest 8-multiple to 54dp) */
val ButtonHeight = 56.dp
